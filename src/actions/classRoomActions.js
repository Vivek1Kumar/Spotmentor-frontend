import { CLASS_NAME } from "./types";

// ======================= Action =================================

export const getContactus = () => dispatch => {
   dispatch({
       type: CLASS_NAME
   })
}